# import streamlit as st

# st.title("Settings")
# st.write("Adjust configurations for LoadRunner Patching Tool.")

# # Example setting
# debug_mode = st.checkbox("Enable Debug Mode")
# st.write(f"Debug Mode is {'ON' if debug_mode else 'OFF'}")

import streamlit as st

st.title("About This Tool")
st.write("""
This is a LoadRunner script processing tool that allows users to add or remove 
Dynatrace request tagging headers. Built with **Streamlit** for easy usability.
""")
